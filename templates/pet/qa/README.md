# Required QA evidence

Before publication, retain:

- `validation-extended.json`
- `chroma-despill-extended.json`
- `run-summary.json`

Also retain compact visual evidence when available: `contact-sheet-extended.png`, `look-directions.png`, and animation previews. Do not publish a passing claim based only on these placeholder instructions.
